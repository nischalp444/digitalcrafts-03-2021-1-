// hello = "Hello World";
// integer = 2
// boolean = false


// while pizza < 8:
// print("pizza")

// for slice in pizza:
//     print(slice)

// Define a variable

//var means variable, function scope
var hello = "Hello World";

//ES6, block scope
let integer = 2

//constant value can not be reassigned, block scope
const boolean = false


// def sum(num1, num2):
//     return num1, num2

function sumNumbers(num1, num2){
// function bode, aka inside the curlies
// for loop syntax
// for (first arg: variabel we define, we initalize to 0)
// for (second arg: our loop condition, is your loop container)
// for (third arg: incrementer)
// lexical scopoing, variables declared outside or insode of a function
// are accessible to children of that function, as long as the variables 
// is defined outside of the child function
    const pizza = 8;
    for (let number = 0; number < array.length; number++){
            console.log(number);

    }
    console.log(numeber);
    return num1 + num2;

}
console.log(sumNumbers(1,2));